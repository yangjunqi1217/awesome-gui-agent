# Sources Scratchpad

This directory is for local source snapshots or temporary research exports.

By default, Markdown snapshots in this directory are ignored by Git to avoid committing large third-party lists verbatim. Keep durable summaries in `docs/` and reading notes in `notes/`.
